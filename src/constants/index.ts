// =============================================================
// الثوابت العامة للتطبيق
// Central place for magic numbers/strings used across the app
// تجميع كل الثوابت هنا يمنع تكرارها (مبدأ DRY) ويسهّل تعديلها
// من مكان واحد بدلاً من البحث عنها في كل ملف
// =============================================================

/** الحد الأقصى لعدد الأفلام المسموح إضافتها إلى المفضلة */
export const MAX_FAVORITES = 10;

/** الرابط الأساسي لواجهة برمجة تطبيقات OMDb */
export const OMDB_BASE_URL = 'https://www.omdbapi.com/';

/**
 * كلمة البحث الافتراضية المستخدمة لملء الصفحة الرئيسية بمحتوى
 * "رائج" عند أول زيارة للمستخدم قبل أن يبحث عن شيء بنفسه
 */
export const DEFAULT_SEARCH_QUERY = 'dark';

/** اسم التطبيق كما يظهر للمستخدم (يُستخدم في عناوين الصفحات) */
export const APP_NAME = 'FILMIX';

/** مدة عرض رسائل التنبيه (Toast) بالميلي ثانية قبل اختفائها تلقائياً */
export const NOTIFICATION_DURATION_MS = 3000;
